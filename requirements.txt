fastapi==0.139.2
uvicorn==0.51.0
websockets==16.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
loguru==0.7.3
httpx==0.28.1
PyJWT==2.13.0

pipecat-ai[deepgram,groq,sarvam,silero,websocket]==1.5.0

SQLAlchemy==2.0.51
alembic==1.18.5
asyncpg==0.31.0
pgvector==0.5.0

arq==0.28.0
redis==5.3.1
hiredis==3.4.0

openai==2.46.0

# Lead lists arrive as spreadsheets. Read-only streaming mode is what keeps a 20,000-row
# sheet off the heap. Present today only as a pipecat transitive dependency, which is not
# something to rely on: the day pipecat drops it the contact import stops importing.
openpyxl==3.1.5
python-multipart==0.0.20
